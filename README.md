# Phishing-URL-Detection
Built a phishing URL classification model using Random Forest, XGBoost, and SVM to detect malicious links. Performed feature extraction and preprocessing to enhance model accuracy and robustness. Evaluated models using performance metrics and selected the best model for reliable predictions.
